# 智能交通系统 — 系统设计文档

> **版本**: v1.0 | **更新日期**: 2026-07-06

---

## 目录

1. [总体架构设计](#1-总体架构设计)
2. [技术选型](#2-技术选型)
3. [模块详细设计](#3-模块详细设计)
4. [数据库设计](#4-数据库设计)
5. [统一接口设计](#5-统一接口设计)
6. [配置说明](#6-配置说明)
7. [部署与目录结构](#7-部署与目录结构)
8. [依赖清单](#8-依赖清单)

---

## 1. 总体架构设计

### 1.1 架构图

```
┌──────────────────────────────────────────────────────────────────┐
│  采集层:  手机浏览器 → /phone 推流页面 → JPEG 帧 (WebSocket) → 云端  │
├──────────────────────────────────────────────────────────────────┤
│  服务层:  FastAPI (统一管理 Port 8000)                             │
│           ├── REST API 路由 (设备/白名单/统计/配置)                │
│           ├── WebSocket 管理 (视频推流接收 + 统一结果推送)         │
│           └── CORS 中间件                                         │
├──────────────────────────────────────────────────────────────────┤
│  业务层:  AI 推理流水线 (Pipeline + Context Property Bag 模式)    │
│           ├── FrameContext (图像帧与数据载体)                      │
│           ├── VehicleDetectionNode (YOLOv8)                       │
│           ├── TrackingNode (SORT)                                 │
│           ├── PlateRecognitionNode (EasyOCR)                      │
│           ├── ViolationDetectionNode (业务逻辑判断)               │
│           └── AnomalyDetectionNode (Grounding DINO 零样本检测)    │
├──────────────────────────────────────────────────────────────────┤
│  数据层:  SQLite + SQLAlchemy (严格主外键与索引)                   │
├──────────────────────────────────────────────────────────────────┤
│  展示层:  React + Vite + Tailwind → 驾驶舱 (Port 5173)            │
└──────────────────────────────────────────────────────────────────┘
```

### 1.2 架构模式：分层架构 + 上下文流水线

#### 通信流转

边端通过 WebSocket 发送二进制帧，服务层统一监听 **8000 端口**，HTTP 请求与 WS 推送**同源管理**。

#### 计算解耦

AI 模块摒弃嵌套调用，采用 **"管道流水线"** 设计。每个检测模型仅作为一个独立的 `Node`，严格遵循：

> **读取前置数据 → 处理 → 追加结果到 Context → 放行**

该设计确保极高的**可扩展性**与**代码整洁度**。新增检测能力只需添加一个 Node，无需修改已有逻辑。

#### 核心特点

| 特点 | 说明 |
|------|------|
| **松耦合** | 各 AI 节点独立开发、独立测试、独立部署 |
| **可观测** | 每个节点的输入输出均可通过 Context 追踪 |
| **可扩展** | 通过 `pipeline.add_node()` 即可插入新的检测能力 |
| **高性能** | 帧级 Context 复用，避免不必要的序列化开销 |
| **按需算力** | 支持节点动态开关与模型延迟加载 (Lazy Loading)，关闭功能时主动释放显存/内存 |

---

## 2. 技术选型

| 组件 | 技术 | 版本 | 理由 |
|------|------|------|------|
| 云端框架 | FastAPI | 0.104.1 | 异步非阻塞，原生 WebSocket 支持，自动生成 OpenAPI 文档 |
| 服务器 | Uvicorn | 0.24.0 | 高性能异步服务器，支持多 worker |
| 车辆检测 | YOLO26n (Ultralytics) | 8.0.215 | 轻量级，COCO 预训练，CPU/轻量 GPU 推理速度快 |
| 目标跟踪 | SORT | — | 基于 Kalman 滤波与匈牙利算法，计算开销极小，适合实时追踪 |
| 字符识别 | EasyOCR | 1.7.1 | 离线可用，中英文混合识别支持良好 |
| 异常物体检测 | Grounding DINO (Tiny) | — | 开放词汇零样本检测，通过文本 Prompt 直接定位突发障碍物与掉落物 |
| 数据库 | SQLite | 3.40.0 | 零配置嵌入式，ORM 提供严谨的模型映射，aiosqlite 支持异步 |
| 前端框架 | React 18 + Vite | — | 组件化开发，热更新极快 |
| 样式方案 | Tailwind CSS | — | 原子化 CSS，快速构建驾驶舱面板 |
| 日志 | Loguru | 0.7.2 | 零配置，结构化日志，彩色输出 |
| 进程监控 | psutil | 5.9.6 | CPU / 内存 / GPU / 磁盘资源采集 |

---

## 3. 模块详细设计

### 3.1 AI 推理流水线核心设计 (Pipeline)

系统采用**单文件单职责**的节点设计。`FrameContext` 作为流动的数据包，贯穿整个生命周期。

#### 3.1.1 FrameContext — 上下文属性包

```python
class FrameContext:
    """上下文属性包：承载一帧的原始数据及所有流经节点的计算结果"""

    def __init__(self, frame_data: np.ndarray, timestamp: float, device_id: str):
        self.frame = frame_data          # 原始图像帧 (BGR numpy array)
        self.timestamp = timestamp        # 帧时间戳
        self.device_id = device_id        # 来源设备 ID
        self.properties: dict = {}        # 属性包字典，由各节点动态写入
```

**属性包约定字段**：

| Key | 类型 | 写入者 | 说明 |
|-----|------|--------|------|
| `vehicle_boxes` | `list[tuple]` | VehicleDetectionNode | 车辆检测框坐标 |
| `vehicle_classes` | `list[str]` | VehicleDetectionNode | 车辆类型标签 |
| `track_ids` | `list[int]` | TrackingNode | SORT 跟踪 ID |
| `plate_numbers` | `list[str]` | PlateRecognitionNode | 识别的车牌号 |
| `plate_confidences` | `list[float]` | PlateRecognitionNode | 车牌识别置信度 |
| `violations` | `list[dict]` | ViolationDetectionNode | 违章事件列表 |
| `road_anomalies` | `list[tuple]` | AnomalyDetectionNode | 路面异常 (box, label) 列表 |

#### 3.1.2 PipelineNode — 流水线节点基类

```python
class PipelineNode:
    """流水线节点基类，支持动态开关与延迟加载"""

    def __init__(self, name: str):
        self.name = name          # 节点唯一标识，用于 API 动态控制
        self.enabled = False      # 默认关闭，不占用算力
        self._model = None        # 模型实例占位符

    def load_model(self):
        """加载模型到显存/内存（由子类实现）"""
        pass

    def unload_model(self):
        """释放模型与显存（由子类实现）"""
        pass

    def toggle(self, state: bool):
        """控制节点开关与显存动态管理"""
        if state and not self.enabled:
            self.load_model()
            self.enabled = True
        elif not state and self.enabled:
            self.unload_model()
            self.enabled = False

    def process(self, context: FrameContext) -> FrameContext:
        """数据处理入口，关闭时直接放行实现 0 算力开销"""
        if not self.enabled:
            return context
        return self._do_process(context)

    def _do_process(self, context: FrameContext) -> FrameContext:
        """实际的推理逻辑（由子类实现）"""
        raise NotImplementedError
```

#### 3.1.3 VehicleDetectionNode — 车辆检测节点

```python
class VehicleDetectionNode(PipelineNode):
    """基于 YOLOv8n 的车辆检测节点"""

    def __init__(self, confidence: float = 0.5):
        self.model = YOLO("yolov8n.pt")
        self.confidence = confidence

    def process(self, context: FrameContext) -> FrameContext:
        # 1. 独立执行 YOLO 检测
        results = self.model(context.frame, conf=self.confidence)
        # 2. 提取车辆相关类别 (car, truck, bus, motorcycle, bicycle)
        boxes, classes = self._extract_vehicle_results(results)
        # 3. 写入属性包，供后续节点使用
        context.properties["vehicle_boxes"] = boxes
        context.properties["vehicle_classes"] = classes
        return context
```

#### 3.1.4 TrackingNode — 目标跟踪节点

```python
class TrackingNode(PipelineNode):
    """基于 SORT 算法的目标跟踪节点"""

    def __init__(self):
        self.tracker = Sort()  # Kalman 滤波 + 匈牙利匹配

    def process(self, context: FrameContext) -> FrameContext:
        boxes = context.properties.get("vehicle_boxes", [])
        if boxes:
            tracked = self.tracker.update(np.array(boxes))
            context.properties["track_ids"] = tracked[:, 4].astype(int).tolist()
        return context
```

#### 3.1.5 PlateRecognitionNode — 车牌识别节点

```python
import gc
import torch

class PlateRecognitionNode(PipelineNode):
    """基于 EasyOCR 的车牌识别节点"""

    def __init__(self, confidence: float = 0.6):
        super().__init__(name="plate_ocr")
        self.confidence = confidence

    def process(self, context: FrameContext) -> FrameContext:
        boxes = context.properties.get("vehicle_boxes", [])
        plates, confidences = [], []
        for box in boxes:
            crop = self._crop_plate_region(context.frame, box)
            results = self._model.readtext(crop)
            plates.append(results[0][1] if results else "")
            confidences.append(results[0][2] if results else 0.0)

        context.properties["plate_numbers"] = plates
        context.properties["plate_confidences"] = confidences
        return context
```

> **注**：其他 AI 节点（如 `VehicleDetectionNode`、`AnomalyDetectionNode` 等）同样需调整为继承新版 `PipelineNode`，实现 `load_model()` 和 `unload_model()` 的结构，以支持动态显存管理。

#### 3.1.6 AnomalyDetectionNode — 异常检测节点

```python
class AnomalyDetectionNode(PipelineNode):
    """基于 Grounding DINO 的零样本异常检测节点"""

    def __init__(self, threshold: float = 0.25):
        model_id = "IDEA-Research/grounding-dino-tiny"
        self.processor = AutoProcessor.from_pretrained(model_id)
        self.model = AutoModelForZeroShotObjectDetection.from_pretrained(model_id)
        self.threshold = threshold

    def process(self, context: FrameContext) -> FrameContext:
        # 使用文本 Prompt 定位异常物体
        prompt = "obstacle . debris . dropped cargo . tire . box . trash . rock"
        image_pil = Image.fromarray(cv2.cvtColor(context.frame, cv2.COLOR_BGR2RGB))
        inputs = self.processor(images=image_pil, text=prompt, return_tensors="pt")
        with torch.no_grad():
            outputs = self.model(**inputs)
        results = self.processor.post_process_grounded_object_detection(
            outputs, input_ids=inputs.input_ids,
            threshold=self.threshold, text_threshold=0.25,
            target_sizes=[image_pil.size[::-1]]
        )[0]
        anomalies = list(zip(results["boxes"], results["labels"]))
        context.properties["road_anomalies"] = anomalies
        return context
```

#### 3.1.7 InferencePipeline — 流水线调度器

```python
class InferencePipeline:
    """流水线调度器：按添加顺序依次执行，支持运行时动态管控"""

    def __init__(self):
        self.nodes: dict[str, PipelineNode] = {}

    def add_node(self, node: PipelineNode):
        self.nodes[node.name] = node

    def toggle_node(self, node_name: str, state: bool):
        """API 触发此方法，动态控制节点的算力分配"""
        if node_name in self.nodes:
            self.nodes[node_name].toggle(state)

    def execute(self, context: FrameContext) -> FrameContext:
        for node in self.nodes.values():
            context = node.process(context)
        return context
```

**使用示例**：

```python
# 构建流水线
pipeline = InferencePipeline()
pipeline.add_node(VehicleDetectionNode(confidence=0.5))
pipeline.add_node(TrackingNode())
pipeline.add_node(PlateRecognitionNode(confidence=0.6))
pipeline.add_node(AnomalyDetectionNode(threshold=0.25))
pipeline.add_node(ViolationDetectionNode(no_parking_zones=[...]))

# 运行时动态控制：开启车牌识别
pipeline.toggle_node("plate_ocr", True)
# 关闭异常检测以释放显存
pipeline.toggle_node("anomaly_detection", False)

# 处理每一帧
context = FrameContext(frame_data=frame, timestamp=ts, device_id="cam_01")
result = pipeline.execute(context)
# 所有检测结果统一在 result.properties 中
```

### 3.2 车辆检测模块 (detect_vehicle.py)

独立脚本，提供基于 Grounding DINO 的车辆检测能力，用于离线测试与调试。

**核心功能**：

- 使用文本 Prompt `"car . truck . bus . motorcycle . bicycle"` 进行零样本检测
- 支持 GPU/CPU 自适应切换
- 检测框按车辆类型着色：
  - 🟢 `car` — 绿色
  - 🔵 `truck` — 蓝色
  - 🔴 `bus` — 红色
  - 🟡 `motorcycle` — 青色
  - 🟣 `bicycle` — 紫色
- 输出带标注的图片，并在底部汇总车辆总数
- 置信度阈值默认为 `0.3`

### 3.3 异常检测模块 (detect_anomaly.py)

独立脚本，提供基于 Grounding DINO 的路面异常检测能力。

**核心功能**：

- 使用文本 Prompt 检测路面障碍物、抛洒物等异常
- 高置信度 (>0.5) 使用红色标注，低置信度使用橙色标注
- 输出带标注的图片
- 置信度阈值默认为 `0.25`

---

## 4. 数据库设计

针对 SQLite 设计，强化主键、外键约束与查询索引，确保数据完整性。

### 4.1 数据库连接

```python
# cloud_server/database/database.py
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession, async_sessionmaker

engine = create_async_engine(
    "sqlite+aiosqlite:///./data/its.db",
    echo=False
)
async_session = async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)
```

### 4.2 数据表设计

#### 4.2.1 plate_records — 车牌识别记录表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | Integer | **PK**, autoincrement | 自增主键 |
| `plate_number` | String(20) | **Index** | 车牌号码 |
| `is_whitelisted` | Boolean | — | 是否为白名单车辆 |
| `timestamp` | DateTime | — | 记录时间 |

#### 4.2.3 vehicle_stats — 车辆密度统计表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | Integer | **PK**, autoincrement | 自增主键 |
| `zone_name` | String(50) | **Index** | 监测区域名称 |
| `vehicle_count` | Integer | — | 车辆数量 |
| `congestion_level` | String(20) | — | 拥堵等级: `low` / `medium` / `high` |
| `timestamp` | DateTime | **Index** | 统计时间 |

**拥堵等级阈值**：

| 等级 | 车辆数 | 说明 |
|------|--------|------|
| `low` | 3-4 辆 | 轻度拥堵 |
| `medium` | 5-8 辆 | 中度拥堵 |
| `high` | >8 辆 | 严重拥堵 |

#### 4.2.4 parking_violations — 违停记录表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | Integer | **PK**, autoincrement | 自增主键 |
| `vehicle_id` | String(50) | — | 车辆跟踪 ID |
| `zone_name` | String(50) | — | 禁停区域名称 |
| `parking_duration` | Float | — | 停留秒数 (>10s 触发告警) |
| `start_time` | DateTime | — | 违停开始时间 |
| `end_time` | DateTime | — | 违停结束时间 |
| `alert_triggered` | Boolean | — | 是否已触发告警 |
| `timestamp` | DateTime | — | 记录时间 |

#### 4.2.5 road_anomalies — 路面异常记录表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | Integer | **PK**, autoincrement | 自增主键 |
| `anomaly_type` | String(50) | — | 异常类型: `obstacle` / `debris` / `dropped_cargo` |
| `confidence` | Float | — | 检测置信度 |
| `location_x` | Float | — | 异常位置 X 坐标 |
| `location_y` | Float | — | 异常位置 Y 坐标 |
| `affected_lane` | String(20) | — | 受影响车道 |
| `timestamp` | DateTime | **Index** | 检测时间 |

#### 4.2.6 system_metrics — 系统资源监控表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | Integer | **PK**, autoincrement | 自增主键 |
| `cpu_usage` | Float | — | CPU 使用率 (%) |
| `gpu_usage` | Float | nullable | GPU 使用率 (%) |
| `memory_usage` | Float | — | 内存使用率 (%) |
| `disk_usage` | Float | — | 磁盘使用率 (%) |
| `network_rx` | Float | — | 网络接收 (Mbps) |
| `network_tx` | Float | — | 网络发送 (Mbps) |
| `video_fps` | Float | — | 视频处理帧率 |
| `video_resolution` | String(20) | — | 视频分辨率 |
| `active_devices` | Integer | — | 活跃设备数 |
| `timestamp` | DateTime | **Index** | 采集时间 |

#### 4.2.7 model_configs — 模型与参数配置表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | Integer | **PK**, autoincrement | 自增主键 |
| `model_name` | String(50) | **Unique** | 模型名称 |
| `task_type` | String(30) | — | 任务类型: `detection` / `recognition` |
| `confidence_threshold` | Float | — | 置信度阈值 |
| `iou_threshold` | Float | — | IoU 阈值 |
| `enabled` | Boolean | — | 是否启用 |
| `params` | JSON | — | 额外参数 (JSON 格式) |
| `updated_at` | DateTime | — | 更新时间 |

---

## 5. 统一接口设计

所有的 HTTP 和 WebSocket 通信均收敛于云端主服务的 **8000 端口**。

### 5.1 WebSocket 端点

| 路径 (Path) | 传输方向 | 数据格式 | 用途说明 |
|-------------|----------|----------|----------|
| `ws://host:8000/ws/stream/{device_id}` | 边端 → 云端 | Binary (JPEG) | 接收边端设备的实时视频流推流 |
| `ws://host:8000/ws/dashboard` | 云端 → 前端 | JSON | 统一推送全量状态 (告警/识别结果/性能指标) |

### 5.2 RESTful API 端点

遵循标准 HTTP 动词与复数资源命名规范。

#### 白名单配置

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/whitelist` | 获取全部白名单车牌 |
| `POST` | `/api/whitelist` | 添加车牌到白名单 |
| `DELETE` | `/api/whitelist/{plate_id}` | 从白名单移除车牌 |

#### 统计图表

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/stats/vehicles?zone=xxx&from=xxx&to=xxx` | 车辆密度历史统计 |
| `GET` | `/api/stats/violations?type=parking&from=xxx&to=xxx` | 违章事件统计 |
| `GET` | `/api/stats/anomalies?from=xxx&to=xxx` | 路面异常统计 |
| `GET` | `/api/stats/system` | 系统资源实时 & 历史指标 |

#### 系统配置

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/configs/models` | 获取各模型参数配置 |
| `PUT` | `/api/configs/models` | 修改置信度、阈值等参数 |
| `GET` | `/api/configs/zones` | 获取禁停区域配置 |
| `PUT` | `/api/configs/zones` | 更新禁停区域多边形 |
| `GET` | `/api/health` | 系统健康检查 |

#### 通用响应结构

```json
{
    "code": 200,
    "message": "success",
    "data": { ... }
}
```

**错误响应**：

```json
{
    "code": 400,
    "message": "Invalid device_id",
    "data": null
}
```

---

## 6. 配置说明

系统配置集中于 `cloud_server/config.py`，关键参数如下：

### 6.1 数据库配置

```python
DATABASE_URL = "sqlite+aiosqlite:///./data/its.db"
```

### 6.2 视频流配置

```python
VIDEO_FRAME_WIDTH = 1280
VIDEO_FRAME_HEIGHT = 720
VIDEO_FPS = 15
VIDEO_STREAM_PORT = 8765       # WebSocket 视频流端口
```

### 6.3 AI 模型配置

```python
YOLO_MODEL_PATH = "models/yolov8n.pt"
YOLO_CONFIDENCE = 0.5          # YOLO 检测置信度阈值
PLATE_CONFIDENCE = 0.6         # 车牌识别置信度阈值
```

### 6.4 禁停区域配置

```python
NO_PARKING_ZONES = [
    {"name": "主路禁停区", "points": [(0.1, 0.1), (0.3, 0.1), (0.3, 0.4), (0.1, 0.4)]},
    {"name": "路口禁停区", "points": [(0.6, 0.5), (0.9, 0.5), (0.9, 0.9), (0.6, 0.9)]},
]
```

禁停区域使用**归一化坐标 (0-1)**，点位按顺时针或逆时针顺序构成封闭多边形。

### 6.5 系统阈值

| 参数 | 值 | 说明 |
|------|---|------|
| `PARKING_THRESHOLD` | 10 秒 | 违停判定时间阈值 |
| `CONGESTION_HIGH` | >8 辆 | 严重拥堵 |
| `CONGESTION_MEDIUM` | 5-8 辆 | 中度拥堵 |
| `CONGESTION_LOW` | 3-5 辆 | 轻度拥堵 |
| `DEVICE_TIMEOUT` | 30 秒 | 设备心跳超时判定离线 |
| `DATA_RETENTION_DAYS` | 30 天 | 历史数据保留天数 |
| `MONITOR_INTERVAL` | 5 秒 | 系统监控采集间隔 |

---

## 7. 部署与目录结构

### 7.1 目录结构

```
Intelligent_Transportation/
├── cloud_server/                    # 云端核心服务
│   ├── main.py                      # FastAPI 入口 (Port 8000)
│   ├── config.py                    # 全局配置
│   ├── pipeline/                    # 核心流水线引擎
│   │   ├── context.py               # FrameContext 定义
│   │   ├── engine.py                # 流水线执行调度器
│   │   └── nodes/                   # 各个独立的计算节点
│   │       ├── detection_node.py    # YOLOv8 车辆检测
│   │       ├── tracking_node.py     # SORT 目标跟踪
│   │       ├── ocr_node.py          # EasyOCR 车牌识别
│   │       ├── anomaly_node.py      # Grounding DINO 异常检测
│   │       └── violation_node.py    # 违章判断 (违停/闯红灯等)
│   ├── api/                         # 接口层
│   │   ├── rest_routes.py           # 规范化的 REST API
│   │   ├── ws_routes.py             # WebSocket 管理
│   │   └── middleware.py            # CORS / 限流 / 鉴权中间件
│   └── database/                    # 数据层
│       ├── connection.py            # 数据库连接管理
│       └── orm_models.py            # SQLAlchemy ORM 模型
├── frontend/                        # 驾驶舱面板 (React + Vite)
│   ├── src/
│   │   ├── components/              # 可复用组件
│   │   ├── pages/                   # 各功能页面
│   │   │   ├── Dashboard.tsx        # 实时概览
│   │   │   ├── VehicleMonitor.tsx   # 车辆监控
│   │   │   ├── AnomalyAlerts.tsx    # 异常告警
│   │   │   └── SystemConfig.tsx     # 系统配置
│   │   └── services/
│   │       ├── api.ts               # REST API 调用
│   │       └── ws.ts                # WebSocket 连接
│   └── tailwind.config.js
├── models/                          # AI 模型权重文件
├── data/                            # 运行时数据目录
│   ├── its.db                       # SQLite 数据库文件
│   └── whitelist.json               # 白名单车牌 JSON
├── detect_vehicle.py                # 离线车辆检测脚本
├── detect_anomaly.py                # 离线异常检测脚本
├── requirements.txt                 # Python 依赖清单
├── README.md                        # 项目说明
└── docs/                            # 文档目录
    └── 系统设计方案v1.0.md
```