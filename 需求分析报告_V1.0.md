# "云边端协同"智慧交通视觉感知系统

## 需求分析报告 V1.0

---

### 文档修订记录

| 版本 | 日期 | 修订内容 | 修订人 |
|------|------|----------|--------|
| V1.0 | 2026-07-06 | 初稿创建 | — |

---

## 1. 引言

### 1.1 编写目的

本文档旨在明确"云边端协同"智慧交通视觉感知系统的功能需求与非功能需求，为后续的系统设计及代码开发提供指导依据。本文档的预期读者为项目开发团队成员、指导教师及系统评审人员。

### 1.2 项目背景

本项目模拟真实智慧交通场景，构建一个基于"云-边-端"协同架构的视觉感知系统。系统旨在通过计算机视觉与深度学习技术，对交通路况进行实时监控、智能分析与预警。系统主要应用于学院704智慧交通沙盘，同时具备泛化至真实校内外道路场景的能力。

---

## 2. 总体架构与运行环境需求

### 2.1 物理与网络拓扑

系统采用物理隔离的三端架构，需满足以下设备协同需求：

#### 2.1.1 边缘端（数据采集 / 初级处理）

- 使用手机作为边缘采集设备。
- 需求包括推流或直接传输视频至云端，并具备在边缘侧执行轻量级AI分析的扩展能力。
- 支持通过移动手机改变监控视角。

#### 2.1.2 云服务端（核心AI算力池）

- 使用一台笔记本作为云服务器。
- 负责拉流或接收前端视频数据，承载主要的大模型或视觉算法推理（AI分析）任务。
- 对视频流进行实时分析并输出结构化感知数据。

#### 2.1.3 前端展示端（交互与监控）

- 使用另一台笔记本作为前端业务平台。
- 负责接收云端分析结果并进行可视化展示及系统调度。
- 提供用户交互界面，支持模型切换、参数配置等操作。

### 2.2 系统架构图（概要）

```
┌──────────────┐     视频流      ┌──────────────────┐     分析结果     ┌──────────────┐
│  边缘端       │ ──────────────> │   云服务端        │ ──────────────> │  前端展示端    │
│  (智能手机)    │ <────────────── │  (高性能PC)       │ <────────────── │  (业务PC)     │
│  视频采集      │   控制指令      │  AI推理引擎       │   查询请求      │  数据可视化    │
└──────────────┘                 └──────────────────┘                 └──────────────┘
```

---

## 3. 功能需求分析 (Functional Requirements)

根据业务场景，系统功能主要划分为"智能视觉感知"、"系统后台管理"与"数据可视化展示"三大模块。

---

### 3.1 智能视觉感知模块

#### FR-1.1 车牌检测与识别

| 项目 | 描述 |
|------|------|
| **优先级** | 高 |
| **需求描述** | 系统需实时处理视频流中的车辆目标，提取并识别车牌信息。识别结果需与本地白名单车辆数据库进行快速比对，进而输出闸机开放决策信号。 |
| **输入** | 视频流中的车辆画面帧 |
| **输出** | 车牌号字符串、匹配结果（白名单/非白名单）、闸机开放决策（通过/拒绝） |
| **备注** | 仅输出决策逻辑，无需对接物理控制硬件 |

**详细要求：**

1. 支持对行驶中及静止车辆的车牌进行检测与字符识别。
2. 本地白名单数据库支持增、删、改、查操作。
3. 识别与比对结果需实时推送至前端展示。
4. 车牌识别结果需持久化存储，支持历史查询。

#### FR-1.2 车辆识别与拥堵热力图生成

| 项目 | 描述 |
|------|------|
| **优先级** | 高 |
| **需求描述** | 系统需检测画面内的所有车辆目标，统计各设定区域内的车辆密度与绝对数量，并据此动态渲染拥堵热力图，直观反映交通拥堵态势。 |
| **输入** | 视频流中的全景画面帧 |
| **输出** | 各区域车辆计数、拥堵等级、拥堵热力图叠加画面 |

**详细要求：**

1. 支持将监控画面划分为多个逻辑区域（如路段、路口等）。
2. 实时统计各区域内车辆目标数量。
3. 根据车辆密度动态计算拥堵等级（如畅通/缓行/拥堵/严重拥堵）。
4. 以热力图形式叠加渲染至原始监控画面，颜色梯度反映拥堵程度。
5. 拥堵数据需持久化存储，支持时序趋势分析。

#### FR-1.3 禁停区车辆跟踪与告警

| 项目 | 描述 |
|------|------|
| **优先级** | 中 |
| **需求描述** | 系统需支持划定虚拟"禁停区域"。对进入该区域的车辆进行持续目标跟踪（Tracking），自动计算其停留时长。当停留时间超过预设阈值时，自动触发"长时间停留告警"事件。 |
| **输入** | 视频流帧 + 用户定义的禁停区域多边形 |
| **输出** | 目标跟踪轨迹、停留时长、告警事件 |

**详细要求：**

1. 支持用户在监控画面中自由绘制虚拟禁停区域（多边形区域）。
2. 对进入禁停区域的车辆分配唯一Track ID并进行持续跟踪。
3. 实时计算每辆车的停留时长（从进入禁停区域开始计时）。
4. 停留时长阈值可由用户配置（默认值建议60秒）。
5. 超出阈值的车辆触发告警，告警信息包含：车辆截图、车牌号（如可识别）、停留时长、位置坐标。
6. 当车辆驶离禁停区域时，停止计时并清除该车告警状态。

#### FR-1.4 道路异常检测

| 项目 | 描述 |
|------|------|
| **优先级** | 中 |
| **需求描述** | 系统需检测路面上不应存在的异常物体。一旦识别异常，需即时生成告警，并在监控画面中标注异常物体的精准位置及受影响的车道。 |
| **输入** | 视频流帧 |
| **输出** | 异常物体边界框、异常类别标签、受影响车道标识、告警事件 |

**详细要求：**

1. 支持检测常见道路异常类型：掉落物、路面异物等。
2. 检测到异常后，在监控画面中以边界框标注异常物体位置。
3. 自动判断异常物体所在车道并高亮显示受影响车道。
4. 即时生成告警事件，推送至前端展示。
5. 异常事件需持久化存储，包含：时间戳、异常截图、位置坐标、车道信息。

---

### 3.2 系统与设备管理模块

#### FR-2.1 硬件与系统状态监控

| 项目 | 描述 |
|------|------|
| **优先级** | 中 |
| **需求描述** | 实时监控云端/边缘端设备的 CPU、GPU 及内存使用率。同时需监控视频流的拉流及传输网络状态，确保链路稳定。 |
| **输入** | 设备系统指标数据 |
| **输出** | 设备状态仪表盘、资源使用率曲线图、网络状态指示 |

**详细要求：**

1. 云端设备监控：实时采集CPU使用率、GPU使用率、内存占用率、磁盘IO等。
2. 边缘设备监控：实时采集手机端CPU、内存、电量等信息。
3. 视频流监控：监控推/拉流的帧率(FPS)、码率、延迟、丢包率等指标。
4. 当资源使用率超过阈值（如CPU > 90%）时触发告警通知。
5. 设备监控数据需持久化存储，支持历史趋势查询。

#### FR-2.2 边缘设备管理

| 项目 | 描述 |
|------|------|
| **优先级** | 中 |
| **需求描述** | 支持边缘设备（手机端）的注册接入、授权管理及在线/离线心跳状态监控。 |
| **输入** | 设备注册信息、心跳信号 |
| **输出** | 设备列表、设备在线状态、设备详情 |

**详细要求：**

1. 设备注册：支持手机端通过扫码或输入注册码的方式接入系统。
2. 授权管理：管理员可审批设备的接入请求，设置设备权限。
3. 心跳监控：边缘设备定期发送心跳包（建议间隔5秒），云端超时检测（建议30秒无心跳判定离线）。
4. 设备列表展示所有已注册设备及其在线/离线状态。
5. 支持管理员强制断开异常设备连接。

#### FR-2.3 AI模型管理

| 项目 | 描述 |
|------|------|
| **优先级** | 中 |
| **需求描述** | 提供模型热切换与配置能力。用户可根据实际场景（如沙盘模式 vs 真实道路模式）选择不同的视觉模型，并支持置信度阈值、NMS等关键超参数的配置。 |
| **输入** | 模型选择指令、超参数配置值 |
| **输出** | 模型切换状态、当前生效配置信息 |

**详细要求：**

1. 模型切换：支持在系统运行期间无感切换不同预置模型（热切换，无需重启服务）。
2. 场景预设：预置"沙盘模式"与"真实道路模式"两套模型及参数方案，支持一键切换。
3. 超参数配置：支持用户调整以下参数：
   - 检测置信度阈值（Confidence Threshold）
   - NMS IoU阈值
   - 输入帧分辨率
   - 推理帧率控制
4. 配置变更后即时生效，无需重启推理服务。
5. 支持用户自定义保存多套参数方案。

---

### 3.3 数据展示与统计查询模块

#### FR-3.1 综合监控看板

| 项目 | 描述 |
|------|------|
| **优先级** | 高 |
| **需求描述** | 提供实时监控大屏，集成闸机监控、拥堵热力图、禁停区监控、道路异常监控等画面。支持通过移动手机改变视角，并支持在界面上快速切换调用的AI模型与视图展示方式。 |
| **输入** | 各模块实时分析结果数据 |
| **输出** | 多视图综合监控界面 |

**详细要求：**

1. 看板布局支持多窗口平铺展示：
   - 主视频流画面（含检测标注叠加）
   - 闸机监控面板（车牌识别结果与通行决策）
   - 拥堵热力图面板
   - 禁停区监控面板
   - 道路异常监控面板
2. 界面上提供快速模型切换下拉菜单或按钮。
3. 告警信息以弹窗或侧边栏形式实时展示，支持声音提醒。
4. 支持全屏模式适配大屏展示场景。

#### FR-3.2 数据统计与历史查询

| 项目 | 描述 |
|------|------|
| **优先级** | 高 |
| **需求描述** | 系统需对感知数据进行持久化存储。支持查询历史检测车牌记录、交通拥堵度的时序变化趋势、违规停车发生次数、道路异常事件的数量及其空间位置分布。 |
| **输入** | 查询条件（时间范围、事件类型等） |
| **输出** | 数据列表、统计图表、趋势折线图、热力图 |

**详细要求：**

1. **历史车牌记录查询：** 支持按时间段、车牌号模糊搜索、通行状态等条件查询历史记录，支持分页展示。
2. **拥堵趋势分析：** 以折线图展示指定时间段内各区域拥堵度的时序变化趋势。
3. **违规停车统计：** 统计指定时间段内禁停区告警次数，支持按区域、时段维度聚合分析。
4. **异常事件分布：** 以热力图或标记图形式展示道路异常事件的空间位置分布。
5. 所有统计结果支持导出为CSV或Excel格式。
6. 支持按日、周、月、年等不同时间粒度进行数据聚合展示。

---

## 4. 非功能需求分析 (Non-Functional Requirements)

### 4.1 实时性与性能需求

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 端到端延迟 | ≤ 3秒 | 从手机采集到前端展示的全链路延迟 |
| 推理帧率 | ≥ 15 FPS | AI模型推理处理帧率 |
| 视频推流帧率 | ≥ 30 FPS | 边缘端推流帧率，保证画面流畅 |
| 视频传输分辨率 | ≥ 360p | 最低分辨率要求 |

- 作为视觉感知系统，端到端（从手机采集到前端展示）的延迟应控制在合理范围内，满足交通监控的实时性要求。
- 推拉流需保证画面流畅，不出现严重丢帧。
- GPU推理需充分利用硬件加速能力，避免成为性能瓶颈。

### 4.2 可靠性需求

- **断线重连：** 边缘设备处于移动或无线网络环境下，系统需具备一定的断线重连和视频流容错处理能力。网络断开后自动重连，恢复后继续推流。
- **视频流容错：** 当视频流出现短暂中断或丢帧时，系统应能自动恢复，不导致系统崩溃。
- **数据持久化：** 系统异常退出时，已采集的分析数据不丢失，需确保数据已写入持久化存储。
- **7×24小时稳定运行：** 系统设计需支持长时间连续运行，具备内存泄漏防护机制。

### 4.3 可扩展性需求

- **算法模块化：** 模块化设计视觉算法，确保未来可以方便地集成更先进的图像分割或目标检测网络。各检测/识别功能以插件形式设计，遵循统一的输入输出接口规范。
- **设备扩展：** 架构支持未来接入多个边缘设备（多路摄像头）同时推流和分析。
- **数据扩展：** 数据库设计需考虑未来数据量增长，支持分表分区策略。
- **功能扩展：** 前后端接口设计遵循RESTful规范，便于未来新增功能模块（如信号灯识别、行人检测等）。

### 4.4 安全性需求

- **设备认证：** 边缘设备接入需通过授权认证机制，防止未授权设备接入系统。
- **数据传输：** 视频流与业务数据在传输过程中宜采用加密传输（如WSS/HTTPS）。
- **访问控制：** 前端管理系统需具备基础的用户登录与权限控制功能。

### 4.5 可维护性需求

- **日志系统：** 系统各模块需输出结构化日志，支持按级别（INFO/WARN/ERROR）过滤，便于问题定位。
- **配置管理：** 系统配置项集中管理，支持通过配置文件或管理界面修改，无需修改代码。
- **文档完善：** 提供清晰的部署文档与API接口文档。

### 4.6 兼容性需求

- **跨平台前端：** 前端展示端宜基于Web技术实现，支持主流浏览器（Chrome、Edge、Firefox）。
- **模型兼容：** AI推理框架需兼容主流模型格式（如ONNX、TensorRT、PyTorch等）。

---

## 5. 数据需求

### 5.1 数据类型与存储

| 数据类型 | 存储方式 | 保留周期 |
|----------|----------|----------|
| 车牌识别记录 | 关系型数据库 | ≥ 90天 |
| 拥堵统计数据 | 关系型数据库 | ≥ 90天 |
| 告警事件记录 | 关系型数据库 | ≥ 180天 |
| 异常事件截图 | 文件存储 / 对象存储 | ≥ 90天 |
| 设备监控数据 | 时序数据库 | ≥ 30天 |
| 系统运行日志 | 日志文件 | ≥ 30天 |

### 5.2 本地白名单数据库

- 存储格式：关系型数据库或结构化文件（JSON/YAML）。
- 字段内容：车牌号、车主信息、有效期限、备注。
- 支持通过前端界面或API进行增、删、改、查操作。

---

## 6. 约束与假设

### 6.1 技术约束

1. 云服务端与前端展示端均为笔记本，需配备NVIDIA独立显卡用于GPU加速推理。
2. 三端设备需处于同一局域网内或通过可靠网络连接。
3. 边缘端智能手机需具备较高质量的后置摄像头。

### 6.2 假设条件

1. 假定学院704智慧交通沙盘场景光照条件良好，无明显遮挡。
2. 假定网络环境稳定，带宽足以支撑高清视频流传输。
3. 假定目标车辆和车牌在画面中清晰可见，分辨率满足检测识别要求。

---

## 7. 验收标准

| 编号 | 验收项 | 验收标准 |
|------|--------|----------|
| AC-1 | 车牌检测与识别 | 在标准光照条件下，车牌识别准确率 ≥ 90% |
| AC-2 | 车辆检测 | 车辆目标检测召回率 ≥ 85% |
| AC-3 | 拥堵热力图 | 能够正确反映各区域车辆密度差异，刷新率 ≥ 1次/秒 |
| AC-4 | 禁停区告警 | 停留超过阈值后10秒内触发告警 |
| AC-5 | 道路异常检测 | 能够识别明显路面异常物体并生成告警 |
| AC-6 | 模型热切换 | 切换操作完成到新模型生效 ≤ 5秒 |
| AC-7 | 端到端延迟 | 全链路延迟（采集→展示）≤ 3秒 |
| AC-8 | 断线重连 | 网络恢复后30秒内自动重连并恢复推流 |
| AC-9 | 综合看板 | 看板各模块数据实时更新，无明显卡顿 |

---

*<div align="center">— 文档结束 —</div>*
