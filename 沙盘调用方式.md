这份文档中您需要提取的第一部分（沙盘摄像头视频流地址）和第二部分（读取并显示摄像头数据参考代码）的文字内容如下：

## **第一部分：沙盘摄像头视频流获取地址**

如果您想测试自己的模型在沙盘好不好用，可以通过下面的方式获取沙盘摄像头的视频流 ：

* **live1**：rtsp://10.126.59.120:8554/live/livel \#桥面  
* **live2**：rtsp://10.126.59.120:8554/live/live2 \# 停车场出口  
* **live3**：rtsp://10.126.59.120:8554/live/live3 \#行人检测  
* **live4**：rtsp://10.126.59.120:8554/live/live4 \#消防车识别  
* **live5**：rtsp://10.126.59.120:8554/live/live5 \#桥出口  
* **live6**：rtsp://10.126.59.120:8554/live/live6 \#桥入口  
* **live7**：rtsp://10.126.59.120:8554/live/live7 \#道路2  
* **live8**：rtsp://10.126.59.120:8554/live/live8 \#隧道(事故识别)  
* **live9**：rtsp://10.126.59.120:8554/live/live9 \#隧道(车辆数量)  
* **live10**：rtsp://10.126.59.120:8554/live/live10 \#道路3  
* **live11**：rtsp://10.126.59.120:8554/live/livell \#停车场入口  
* **live12**：rtsp://10.126.59.120:8554/live/live12 \#道路1

## **第二部分：读取并显示摄像头数据参考代码**

读取并显示摄像头数据参考代码如下 ：（*注：已为您修复原OCR文本中因换行错位导致循环/判断逻辑混乱的问题*）


import cv2
def main():
    rtsp_url = "rtsp://10.126.59.120:8554/live/live1"
    # 使用 FFMPEG 后端并开启 TCP 传输，减少花屏/延迟
    cap = cv2.VideoCapture(rtsp_url, cv2.CAP_FFMPEG)
    cap.set(cv2.CAP_PROP_BUFFERSIZE, 1)
    if not cap.isOpened():
        print(f"无法打开视频流: {rtsp_url}")
        return
    print(f"成功连接到视频流: {rtsp_url}")
    print("按 q 退出，按 s 截图保存为 snapshot.jpg")
    while True:
        ret, frame = cap.read()
        if not ret:
            print("读取帧失败，流可能已断开")
            break
        cv2.imshow("RTSP Stream", frame)
        key = cv2.waitKey(1) & 0xFF
        if key == ord("q"):
            break
        elif key == ord("s"):
            cv2.imwrite("snapshot.jpg", frame)
            print("已保存截图: snapshot.jpg")
    cap.release()
    cv2.destroyAllWindows()
if __name__ == "__main__":
    main()