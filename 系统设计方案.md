# 智能交通系统 — 系统设计文档

---

## 目录

1. [总体架构设计](#1-总体架构设计)
2. [技术选型](#2-技术选型)
3. [模块详细设计](#3-模块详细设计)
4. [数据库设计](#4-数据库设计)
5. [统一接口设计](#5-统一接口设计)
6. [配置说明](#6-配置说明)
7. [部署与目录结构](#7-部署与目录结构)
---

## 1. 总体架构设计

### 1.1 架构图

![功能及技术架构图](./功能及技术架构图.png)

### 1.2 架构模式：分层架构 + 上下文流水线

#### 通信流转

边端通过 WebSocket 发送二进制帧，服务层统一监听 **8000 端口**，HTTP 请求与 WS 推送**同源管理**。

#### 计算解耦

AI 模块摒弃嵌套调用，采用 **"管道流水线"** 设计。每个检测模型仅作为一个独立的 `Node`，严格遵循：

> **读取前置数据 → 处理 → 追加结果到 Context → 放行**

该设计确保极高的**可扩展性**与**代码整洁度**。新增检测能力只需添加一个 Node，无需修改已有逻辑。

#### 核心特点

| 特点 | 说明 |
|------|------|
| **松耦合** | 各 AI 节点独立开发、独立测试、独立部署 |
| **可观测** | 每个节点的输入输出均可通过 Context 追踪 |
| **可扩展** | 通过 `pipeline.add_node()` 即可插入新的检测能力 |
| **高性能** | 帧级 Context 复用，避免不必要的序列化开销 |
| **按需算力** | 支持节点动态开关与模型延迟加载 (Lazy Loading)，关闭功能时主动释放显存/内存 |

---

## 2. 技术选型

### 2.1 技术栈总览

| 组件 | 技术 | 版本 | 理由 |
|------|------|------|------|
| 云端框架 | FastAPI | 0.104.1 | 异步非阻塞，原生 WebSocket 支持，自动生成 OpenAPI 文档 |
| 服务器 | Uvicorn | 0.24.0 | 高性能异步服务器，支持多 worker |
| 车辆检测 | YOLO26n (Ultralytics) | 8.0.215 | 轻量级，COCO 预训练，CPU/轻量 GPU 推理速度快 |
| 目标跟踪 | SORT | — | 基于 Kalman 滤波与匈牙利算法，计算开销极小，适合实时追踪 |
| 字符识别 | HyperLPR | — | 国产开源高性能车牌识别框架，专为中国车牌设计，支持多省车牌快速精准识别 |
| 异常物体检测 | Depth Anything V2 | — | 单目深度估计模型，通过深度异常分析检测路面障碍物与掉落物，无需类别预定义 |
| 数据库 | SQLite | 3.40.0 | 零配置嵌入式，ORM 提供严谨的模型映射，aiosqlite 支持异步 |
| 前端框架 | React 18 + Vite | — | 组件化开发，热更新极快 |
| 样式方案 | Tailwind CSS | — | 原子化 CSS，快速构建驾驶舱面板 |
| 热力图渲染 | Leaflet.js + heatmap.js | — | 轻量级地图可视化，热力图层叠加，Canvas 高性能渲染 |
| 日志 | Loguru | 0.7.2 | 零配置，结构化日志，彩色输出 |
| 进程监控 | psutil | 5.9.6 | CPU / 内存 / GPU / 磁盘资源采集 |

### 2.2 各组件选型详述

#### 2.2.1 FastAPI — 云端 Web 框架

| 维度 | 说明 |
|------|------|
| **选型理由** | 原生 async/await 异步支持，适合 I/O 密集型的视频流推拉与 WebSocket 长连接管理 |
| **备选方案** | Flask + Flask-SocketIO / Django Channels / Node.js Express |
| **对比分析** | Flask 缺少原生异步和 WebSocket 支持，需额外引入三方库；Django 过于沉重，不适合微服务场景；FastAPI 凭借 Pydantic 自动校验 + Swagger UI 自动文档，大幅降低前后端联调成本 |
| **核心优势** | 自动 OpenAPI 文档生成、Pydantic 类型校验、依赖注入系统、内置 CORS/限流中间件支持、接近 Node.js/Go 的吞吐性能 |
| **适用边界** | 本系统服务端口收敛为单端口 (8000)，HTTP REST 与 WebSocket 同源管理，天然适合 FastAPI 的统一路由机制 |

#### 2.2.2 Uvicorn — ASGI 服务器

| 维度 | 说明 |
|------|------|
| **选型理由** | 基于 uvloop 和 httptools 构建，是目前 Python 生态中最快的 ASGI 服务器实现 |
| **备选方案** | Gunicorn + uvicorn workers / Hypercorn / Daphne |
| **对比分析** | Gunicorn 为 WSGI 设计，需通过 uvicorn worker 桥接；Hypercorn 支持 HTTP/2 但社区与性能不如 Uvicorn；Daphne 主要面向 Django Channels |
| **核心优势** | 支持 `--workers` 多进程部署、`--reload` 热重载开发模式、与 FastAPI 深度集成（FastAPI 官方推荐） |
| **部署建议** | 生产环境使用 `uvicorn main:app --host 0.0.0.0 --port 8000 --workers 4` |

#### 2.2.3 YOLO26n — 车辆检测模型

| 维度 | 说明 |
|------|------|
| **选型理由** | Ultralytics YOLO 系列最新版本，n (nano) 变体在 CPU 上即可实现实时推理 |
| **备选方案** | YOLOv8n / YOLOv5s / Faster R-CNN / RT-DETR / EfficientDet |
| **对比分析** | Faster R-CNN 精度高但推理速度慢 (>50ms/帧)，不适合实时场景；RT-DETR 精度优秀但模型体积大；YOLOv8n/YOLOv5s 是成熟方案，YOLO26n 在保持 nano 体量的前提下进一步提升 mAP |
| **核心优势** | 模型仅 ~6MB，CPU 推理 ~30ms/帧；COCO 预训练涵盖 car/truck/bus/motorcycle/bicycle 五类车辆；支持 ONNX/TensorRT/OpenVINO 导出；Python API 三行代码完成推理 |
| **推理配置** | 置信度阈值默认 `0.5`，IoU 阈值 `0.45`，可根据场景光照动态调整 |
| **升级路径** | 若需更高精度可替换为 YOLO26s/m/l；部署移动端可导出为 NCNN 或 TFLite 格式 |

#### 2.2.4 SORT — 多目标跟踪算法

| 维度 | 说明 |
|------|------|
| **选型理由** | 最简单高效的在线多目标跟踪算法，单帧处理耗时 <1ms，完美契合实时视频流场景 |
| **备选方案** | DeepSORT / ByteTrack / StrongSORT / BoT-SORT |
| **对比分析** | DeepSORT 引入 ReID 特征网络，精度提升但推理开销增大 5-10 倍；ByteTrack 利用低分检测框改善遮挡场景，但仍比 SORT 重；SORT 仅依赖 Kalman 滤波的运动预测与匈牙利算法的 IoU 匹配 |
| **核心优势** | 纯运动模型，无需 GPU；每帧 <1ms 延迟；与 YOLO 检测框无缝对接；核心逻辑 <200 行代码 |
| **局限与应对** | ID Switch 在严重遮挡时较频繁 → 可后续引入 IoU + 中心距离加权匹配作为增强 |

**SORT 算法流程**：

```
1. Kalman 预测: 对每个已跟踪目标，预测其在当前帧的位置
   state = [x, y, s, r, dx, dy, ds]   (中心坐标/面积/宽高比/速度分量)

2. 数据关联: 构建预测框与检测框的 IoU 成本矩阵
   cost[i][j] = 1 - IoU(pred_box_i, det_box_j)

3. 匈牙利匹配: 求解最小成本分配，得到跟踪 ID ↔ 检测框映射

4. Kalman 更新: 用匹配成功的检测框修正跟踪器状态

5. 生命周期管理:
   - 新检测 (未匹配): 创建跟踪器，需连续命中 3 帧确认
   - 丢失跟踪 (未匹配): 标记 tentative，超过 1 帧则删除
```

#### 2.2.5 HyperLPR — 车牌字符识别

| 维度 | 说明 |
|------|------|
| **选型理由** | 国产开源高性能车牌识别框架，专为中国车牌设计，支持蓝牌、绿牌（新能源）、黄牌等多种类型，识别速度快、准确率高 |
| **备选方案** | PaddleOCR / Tesseract OCR / EasyOCR / 云端 OCR API |
| **对比分析** | Tesseract 对中文车牌识别效果差；PaddleOCR 精度高但依赖 PaddlePaddle 框架，安装体积大；EasyOCR 虽支持多语言但对车牌专用场景精度不如 HyperLPR；云端 API 需要网络且产生调用费用；HyperLPR 作为专用车牌识别方案，在模型体量、推理速度和车牌识别准确率上达到最优平衡 |
| **核心优势** | 专为中国车牌优化的轻量级 CNN+CTC 模型，支持 10+ 省份车牌快速识别，CPU 推理 ~50-100ms/车牌，支持端到端车牌检测+识别一体化 Pipeline |
| **增强策略** | 模糊/倾斜车牌 → Pipeline 中增加预处理：透视变换校正 + 自适应阈值二值化 + 车牌格式正则过滤 |

#### 2.2.6 Depth Anything V2 — 异常物体检测

| 维度 | 说明 |
|------|------|
| **选型理由** | 单目深度估计 SOTA 模型，通过推断场景深度信息来检测路面异常物体，无需为每种异常类别预定义标签，具有良好的泛化能力 |
| **备选方案** | Grounding DINO / OWL-ViT / CLIP + SAM / YOLO-World / 传统分割算法 |
| **对比分析** | Grounding DINO 等开放词汇检测模型需文本 Prompt 引导，对罕见或未知异常物体存在漏检风险；传统分割算法无法区分前景物体类别；Depth Anything V2 通过单帧图像即可估计像素级深度，路面上凸起或凹陷的异常物体在深度图中表现为局部深度突变区域，无需预定义异常类别即可检出 |
| **检测原理** | 输入路面场景 RGB 图像 → Depth Anything V2 推理输出深度图 → 对深度图进行局部梯度分析，检测深度不连续区域 → 与背景路面深度拟合平面比较，标记偏离阈值的高差区域作为异常候选 |
| **推理开销** | ViT-Small 变体 GPU 推理 ~30-50ms/帧，显著轻于 Grounding DINO；支持**动态开关节点**，在不需要时主动释放显存 |

#### 2.2.7 SQLite + SQLAlchemy + aiosqlite — 数据持久化

| 维度 | 说明 |
|------|------|
| **选型理由** | 零配置嵌入式数据库，无需独立数据库服务，完美匹配单机/边缘部署场景 |
| **备选方案** | PostgreSQL / MySQL / MongoDB / Redis |
| **对比分析** | PostgreSQL/MySQL 需独立服务进程，运维成本高；MongoDB 适合非结构化但本系统数据模式固定；SQLite WAL 模式支持并发读，对本系统写入量级（每分钟数十条）绰绰有余 |
| **核心优势** | 单文件存储，备份仅需复制 `its.db`；SQLAlchemy ORM 类型安全；aiosqlite 支持 FastAPI 异步会话 |
| **升级路径** | 需分布式部署时，修改 `DATABASE_URL` 一行配置即可切换到 PostgreSQL（ORM 驱动无关性） |

#### 2.2.8 React 18 + Vite + Tailwind CSS — 前端技术栈

| 维度 | 说明 |
|------|------|
| **选型理由** | React 18 并发特性适合实时数据驱动的驾驶舱面板；Vite 毫秒级 HMR；Tailwind CSS 原子化样式加速开发 |
| **备选方案** | Vue 3 + Nuxt / Svelte / Angular / Next.js |
| **核心优势** | React Hooks + WebSocket 实时推送天然契合；Vite 秒级冷启动；Tailwind JIT 按需生成 CSS |
| **组件规划** | `Dashboard` / `VehicleMonitor` / `CongestionHeatmap` / `AnomalyAlerts` / `SystemConfig` |

#### 2.2.9 Leaflet.js + heatmap.js — 拥堵热力图渲染

| 维度 | 说明 |
|------|------|
| **选型理由** | Leaflet 是最轻量的开源地图库 (~40KB gzipped)，heatmap.js 提供 Canvas 高性能热力图层 |
| **备选方案** | Mapbox GL JS / OpenLayers / ECharts 热力图 / deck.gl |
| **核心优势** | 离线可用 (OpenStreetMap 瓦片)；1000+ 数据点时保持 60fps；支持自定义渐变配色映射拥堵等级 |

#### 2.2.10 Loguru + psutil — 日志与监控

| 组件 | 核心优势 |
|------|----------|
| **Loguru** | 零配置替代标准库 logging，彩色结构化输出、文件自动轮转/压缩、异常堆栈美化 |
| **psutil** | 跨平台 CPU/内存/磁盘/网络采集，纯 Python 实现，每 5 秒写入 `system_metrics` 表供前端展示 |

---

### 2.3 基于道路拓扑的拥堵热力图可视化设计

#### 2.3.1 设计目标

利用车辆检测 (YOLO26n) 与目标跟踪 (SORT) 的实时输出，将车辆轨迹点匹配到道路拓扑路网上，基于路网拓扑结构进行密度统计，在前端以热力图形式直观呈现道路拥堵分布，辅助交通管理者快速定位拥堵路段。

#### 2.3.2 核心思路：从画面网格到道路拓扑

传统方案将画面划分为均匀像素网格进行统计，存在以下局限：

- **空间语义缺失**：像素网格不区分道路与非道路区域，建筑物、绿化带中的检测噪声会影响统计结果
- **尺度不一致**：近处车辆占据像素多、远处车辆占据像素少，统一网格无法反映真实道路空间密度
- **跨相机不连通**：各相机画面网格相互独立，无法形成全网统一的路况视图

**改进方案**：将车辆轨迹点映射到预定义的道路拓扑路网上，以道路路段 (Link) 为基本统计单元，计算每条路段的车辆密度。该方案具有以下优势：

| 优点 | 说明 |
|------|------|
| **空间语义正确** | 仅统计道路路段上的车辆，天然排除非道路区域噪声 |
| **物理尺度统一** | 密度 = 车辆数 / 路段物理长度 (辆/km)，不同路段可直接比较 |
| **跨相机融合** | 多个相机覆盖的路网区域可在同一拓扑图中汇总，形成区域级拥堵视图 |

#### 2.3.3 道路拓扑路网建模

将道路网络建模为有向图 `G = (V, E)`：

| 元素 | 表示 | 属性 |
|------|------|------|
| 节点 V | 交叉口 / 卡口位置 | 经纬度 `(lng, lat)`，节点 ID |
| 边 E (路段 Link) | 两节点间道路段 | `link_id`、`length` (物理长度，m)、`direction` (单向/双向)、`lanes` (车道数) |

**相机视野 → 路段映射**：每个监控相机对应其视野内覆盖的路段集合，车辆检测框中心点通过**单应性变换 (Homography)** 投影到路面平面，再通过最近邻匹配归属到具体路段 Link。

#### 2.3.4 密度统计算法一：简单线密度法 (Simple Linear Density)

**原理**：直接统计一段时间窗口内匹配到某条路段上的车辆轨迹点总数，然后除以该路段的物理长度。

**计算步骤**：

1. 滑动窗口收集每条路段 Link 上的车辆轨迹点（以 SORT 跟踪 ID 去重，每车每秒计 1 次）
2. 窗口时长 `W` 秒内的轨迹点总数记为 `N_link`
3. 路段密度：`Density(link) = N_link / (length(link) / 1000)`，单位：辆/km

```python
class LinearDensityEngine:
    """简单线密度法：基于道路拓扑的拥堵密度计算"""

    def __init__(self, road_graph: RoadGraph, window_sec: float = 300.0):
        """
        road_graph: 道路拓扑图 G = (V, E)
        window_sec: 滑动窗口时长 (默认 5 分钟)
        """
        ...

    def map_vehicle_to_link(self, vehicle_position, camera_id) -> Optional[str]:
        """将车辆路面坐标映射到最近的路段 Link ID"""
        ...

    def update(self, track_id: int, vehicle_position, timestamp: float, camera_id: str):
        """每帧调用：将车辆位置匹配到路段，追加轨迹点到时间窗口"""
        ...

    def get_link_density(self, link_id: str) -> float:
        """返回指定路段的当前密度值 (辆/km)"""
        ...

    def get_all_densities(self) -> dict[str, float]:
        """返回所有路段的密度值"""
        ...
```

**核心逻辑**：

1. `map_vehicle_to_link`: 将车辆路面坐标 (经单应性投影) 匹配到最近的 Link
2. `update`: 将匹配成功后的轨迹点 (track_id, link_id, timestamp) 追加到滑动窗口
3. `get_link_density`: 统计窗口内 `link_id` 上的唯一轨迹点数，除以该路段物理长度 (km)

**优点**：计算简单、工程实现鲁棒、结果直观可解释。**缺点**：不考虑相邻路段间的空间关联，密度在路段边界处可能出现不连续。

#### 2.3.5 密度统计算法二：网络核密度估计 (Network KDE)

**原理**：引入核函数（如高斯核），以网络空间中的最短路径距离替代欧氏距离，在道路拓扑网络上进行核密度光滑估计。

**与传统 KDE 的区别**：传统 KDE 使用二维欧氏距离，无法反映道路拓扑约束——空间上临近但分属不同道路的两辆车在实际路网中可能相距甚远；NKDE 使用**沿道路拓扑网络的最短路径长度**作为距离度量，密度估计更符合真实交通特性。

**计算公式**：

$$\lambda(s) = \sum_{i=1}^{n} K\left(\frac{d_{net}(s, s_i)}{h}\right)$$

其中：
- $\lambda(s)$：路网位置 $s$ 处的密度估计值
- $d_{net}(s, s_i)$：位置 $s$ 与第 $i$ 个轨迹点 $s_i$ 之间沿路网的最短路径距离
- $h$：核带宽参数，控制平滑程度
- $K(\cdot)$：核函数，选用高斯核 $K(u) = \frac{1}{\sqrt{2\pi}} e^{-u^2/2}$

```python
class NetworkKDEEngine:
    """网络核密度估计 (NKDE)：基于道路拓扑网络的核密度计算"""

    def __init__(self, road_graph: RoadGraph, bandwidth: float = 200.0,
                 window_sec: float = 300.0):
        """
        road_graph: 道路拓扑图，支持 Dijkstra 最短路径查询
        bandwidth:  高斯核带宽 (沿路网距离，单位 m)
        window_sec: 滑动窗口时长
        """
        ...

    def _shortest_path_distance(self, link_a: str, offset_a: float,
                                 link_b: str, offset_b: float) -> float:
        """计算路网中两点沿道路拓扑的最短路径长度 (Dijkstra)"""
        ...

    def _gaussian_kernel(self, d_net: float) -> float:
        """高斯核函数，距离 d_net 越大权重越小"""
        ...

    def compute_density_at(self, link_id: str, offset: float) -> float:
        """计算路网中指定位置 (link_id, offset) 处的 NKDE 密度值"""
        ...

    def generate_heatmap_samples(self, sample_interval: float = 50.0
                                  ) -> list[tuple[float, float, float]]:
        """沿路网等距采样，返回 (lng, lat, density) 列表供前端渲染"""
        ...
```

**核心逻辑**：

1. `_shortest_path_distance`: 基于 Dijkstra 算法计算路网上任意两点沿道路拓扑的最短路径长度，作为核函数输入的距离参数
2. `_gaussian_kernel`: 输入路网距离 $d_{net}$，输出核权重；链路距离越近、密度贡献越大
3. `compute_density_at`: 对窗口内所有轨迹点，分别计算到目标位置的路网距离，代入高斯核累加得到密度
4. `generate_heatmap_samples`: 沿路网以固定间距采样密度值，生成经纬度 + 密度三元组，供 Leaflet 前端渲染热力图

**NKDE vs 简单线密度法对比**：

| 维度 | 简单线密度法 | 网络核密度估计 (NKDE) |
|------|-------------|----------------------|
| **计算复杂度** | O(n)，仅统计路段内点数 | O(n²·log\|V\|)，需计算全轨迹点对路网距离 |
| **空间连续性** | 路段边界处不连续 | 核函数平滑，路段间密度自然过渡 |
| **统计粒度** | 路段级 (整条 Link 一个值) | 亚路段级 (沿路段等距采样) |
| **适用场景** | 实时监控、粗粒度拥堵概览 | 精细化分析、拥堵热力分布可视化 |
| **推荐使用** | 作为默认/实时方案 | 按需开启（如 5 分钟周期刷新） |

#### 2.3.6 拥堵等级颜色映射

| 等级 | 密度阈值 | 颜色 | HEX | 含义 |
|------|----------|------|-----|------|
| `normal` | 0-10 辆/km | 🟢 绿色 | `#4CAF50` | 道路畅通 |
| `low` | 10-25 辆/km | 🟡 黄色 | `#FFEB3B` | 轻度拥堵 |
| `medium` | 25-50 辆/km | 🟠 橙色 | `#FF9800` | 中度拥堵 |
| `high` | >50 辆/km | 🔴 红色 | `#F44336` | 严重拥堵 |



---

## 3. 模块详细设计

![功能模块图](./功能模块图.png)

### 3.1 AI 推理流水线核心设计 (Pipeline)

系统采用**单文件单职责**的节点设计。`FrameContext` 作为流动的数据包，贯穿整个生命周期。

#### 3.1.1 FrameContext — 上下文属性包

```python
class FrameContext:
    """上下文属性包：承载一帧的原始数据及所有流经节点的计算结果"""
    frame: np.ndarray       # 原始图像帧 (BGR)
    timestamp: float        # 帧时间戳
    device_id: str          # 来源设备 ID
    properties: dict        # 属性包字典，由各节点动态写入
```

**属性包约定字段**：

| Key | 类型 | 写入者 | 说明 |
|-----|------|--------|------|
| `vehicle_boxes` | `list[tuple]` | VehicleDetectionNode | 车辆检测框坐标 |
| `vehicle_classes` | `list[str]` | VehicleDetectionNode | 车辆类型标签 |
| `track_ids` | `list[int]` | TrackingNode | SORT 跟踪 ID |
| `plate_numbers` | `list[str]` | PlateRecognitionNode | 识别的车牌号 |
| `plate_confidences` | `list[float]` | PlateRecognitionNode | 车牌识别置信度 |
| `violations` | `list[dict]` | ViolationDetectionNode | 违章事件列表 |
| `road_anomalies` | `list[tuple]` | AnomalyDetectionNode | 路面异常 (box, label) 列表 |

#### 3.1.2 PipelineNode — 流水线节点基类

```python
class PipelineNode:
    """流水线节点基类，支持动态开关与延迟加载"""
    name: str               # 节点唯一标识
    enabled: bool = False   # 默认关闭，不占用算力
    _model = None           # 模型实例占位符

    def load_model(self):   ...
    def unload_model(self): ...
    def toggle(self, state: bool): ...    # 开关控制 + 显存动态管理
    def process(self, context: FrameContext) -> FrameContext:
        """关闭时直接放行 (0 算力开销)，开启时调用 _do_process"""
        ...
    def _do_process(self, context: FrameContext) -> FrameContext:
        """由子类实现的推理逻辑"""
        raise NotImplementedError
```

#### 3.1.3 VehicleDetectionNode — 车辆检测节点

```python
class VehicleDetectionNode(PipelineNode):
    """基于 YOLOv8n 的车辆检测节点"""
    # 核心流程: YOLO 检测 → 提取车辆类别 (car/truck/bus/motorcycle/bicycle)
    # 写入: context.properties["vehicle_boxes"], ["vehicle_classes"]
    ...
```

#### 3.1.4 TrackingNode — 目标跟踪节点

```python
class TrackingNode(PipelineNode):
    """基于 SORT 算法的目标跟踪节点 (Kalman 滤波 + 匈牙利匹配)"""
    # 读取 vehicle_boxes → SORT.update → 写入 track_ids
    ...
```

#### 3.1.5 PlateRecognitionNode — 车牌识别节点

```python
class PlateRecognitionNode(PipelineNode):
    """基于 HyperLPR 的车牌识别节点"""
    # 读取 vehicle_boxes → 裁剪车牌区域 → HyperLPR 端到端检测+识别 → 写入 plate_numbers, plate_confidences
    ...
```

> **注**：所有 AI 节点均需继承 `PipelineNode`，实现 `load_model()` / `unload_model()` 以支持动态显存管理。

#### 3.1.6 AnomalyDetectionNode — 异常检测节点

```python
class AnomalyDetectionNode(PipelineNode):
    """基于 Depth Anything V2 的深度异常检测节点"""
    # RGB 帧 → Depth Anything V2 深度估计 → 深度图局部梯度分析 → 标记深度突变区域 → 写入 road_anomalies [(box, label), ...]
    ...
```

#### 3.1.7 InferencePipeline — 流水线调度器

```python
class InferencePipeline:
    """流水线调度器：按添加顺序依次执行，支持运行时动态管控"""
    nodes: dict[str, PipelineNode]

    def add_node(self, node: PipelineNode): ...
    def toggle_node(self, node_name: str, state: bool): ...  # API 动态控制
    def execute(self, context: FrameContext) -> FrameContext:
        """遍历所有已开启节点，链式处理 context"""
        ...
```

### 3.2 车辆检测模块 (detect_vehicle.py)

独立脚本，提供基于 Grounding DINO 的车辆检测能力，用于离线测试与调试。

**核心功能**：

- 使用文本 Prompt `"car . truck . bus . motorcycle . bicycle"` 进行零样本检测
- 支持 GPU/CPU 自适应切换
- 检测框按车辆类型着色：
  - 🟢 `car` — 绿色
  - 🔵 `truck` — 蓝色
  - 🔴 `bus` — 红色
  - 🟡 `motorcycle` — 青色
  - 🟣 `bicycle` — 紫色
- 输出带标注的图片，并在底部汇总车辆总数
- 置信度阈值默认为 `0.3`

### 3.3 异常检测模块 (detect_anomaly.py)

独立脚本，提供基于 Depth Anything V2 的路面异常检测能力。

**核心功能**：

- 使用 Depth Anything V2 对路面场景进行单目深度估计
- 对深度图进行局部梯度分析，检测深度不连续区域作为异常候选
- 与背景路面深度拟合平面比较，标记偏离阈值的高差区域
- 高置信度异常区域使用红色标注，低置信度使用橙色标注
- 输出带标注的图片
- 深度异常阈值默认为 `0.3` (相对深度偏差)

### 3.4 数据延迟处理与原子连续性的平衡

系统延迟主要来源于两处：**云边端视频流推送的网络波动**与**各 AI 节点计算开销的差异**。设计目标是在不显著增加端到端延迟的前提下，保证推理结果的时效性与完整性。

**一、关键帧自适应抽取**

| 帧类型 | 处理节点 | 间隔 | 说明 |
|--------|----------|------|------|
| **key (关键帧)** | 全链路推理 | 每 15 帧一次 | 执行检测→跟踪→OCR→违章→异常，产生完整记录 |
| **track (跟踪帧)** | 检测 + 跟踪 | 其余帧 | 仅更新车辆位置和跟踪 ID，SORT 插值维持连续性 |
| **skip (丢弃帧)** | 无 | 流水线过载时 | 直接放行，前端复用上一帧结果，避免队列堆积 |

> **关键帧识别方法备选**：除上述固定间隔策略外，可考虑以下两种方案以进一步提升关键帧选取质量：
>
> **方案 A — 边端降帧率抽帧**：在边端 (手机浏览器) 推流阶段直接降低采集帧率，例如从 30fps 降至 10fps。HTML5 `getUserMedia` 可通过 `ideal` 帧率约束控制，或以定时器按固定间隔从 `<video>` 抓取 `canvas` 帧。优点：从源头减少上行带宽与云端计算压力，实现最简单；缺点：均匀降帧可能漏掉瞬时事件（如车辆快速驶过）。
>
> **方案 B — 轻量模型识别关键帧**：在边端或云端前置一个极轻量的关键帧判别器，仅在场景发生显著变化时才触发全链路推理。可选方案包括：帧间差分法 (MOG2 背景减除) 判断画面运动幅度、SSIM 结构相似度阈值过滤冗余帧、或 MobileNetV3-Small 分类器做二分类（有效帧/冗余帧）。优点：按需触发，兼顾精度与效率；缺点：引入额外计算开销，边端 JS 环境运行模型能力有限，更适合云端前置。

## 4. 数据库设计

![ER图](./ER图.png)

针对 SQLite 设计，强化主键、外键约束与查询索引，确保数据完整性。

### 4.1 数据库连接

```python
# cloud_server/database/database.py
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession, async_sessionmaker

engine = create_async_engine(
    "sqlite+aiosqlite:///./data/its.db",
    echo=False
)
async_session = async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)
```

### 4.2 数据表设计

#### 4.2.1 plate_records — 车牌识别记录表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | Integer | **PK**, autoincrement | 自增主键 |
| `plate_number` | String(20) | **Index** | 车牌号码 |
| `is_whitelisted` | Boolean | — | 是否为白名单车辆 |
| `timestamp` | DateTime | — | 记录时间 |

#### 4.2.3 vehicle_stats — 车辆密度统计表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | Integer | **PK**, autoincrement | 自增主键 |
| `zone_name` | String(50) | **Index** | 监测区域名称 |
| `vehicle_count` | Integer | — | 车辆数量 |
| `congestion_level` | String(20) | — | 拥堵等级: `low` / `medium` / `high` |
| `timestamp` | DateTime | **Index** | 统计时间 |

**拥堵等级阈值**：

| 等级 | 车辆数 | 说明 |
|------|--------|------|
| `low` | 3-4 辆 | 轻度拥堵 |
| `medium` | 5-8 辆 | 中度拥堵 |
| `high` | >8 辆 | 严重拥堵 |

#### 4.2.4 parking_violations — 违停记录表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | Integer | **PK**, autoincrement | 自增主键 |
| `vehicle_id` | String(50) | — | 车辆跟踪 ID |
| `zone_name` | String(50) | — | 禁停区域名称 |
| `parking_duration` | Float | — | 停留秒数 (>10s 触发告警) |
| `start_time` | DateTime | — | 违停开始时间 |
| `end_time` | DateTime | — | 违停结束时间 |
| `alert_triggered` | Boolean | — | 是否已触发告警 |
| `timestamp` | DateTime | — | 记录时间 |

#### 4.2.5 road_anomalies — 路面异常记录表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | Integer | **PK**, autoincrement | 自增主键 |
| `anomaly_type` | String(50) | — | 异常类型: `obstacle` / `debris` / `dropped_cargo` |
| `confidence` | Float | — | 检测置信度 |
| `location_x` | Float | — | 异常位置 X 坐标 |
| `location_y` | Float | — | 异常位置 Y 坐标 |
| `affected_lane` | String(20) | — | 受影响车道 |
| `timestamp` | DateTime | **Index** | 检测时间 |

#### 4.2.6 system_metrics — 系统资源监控表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | Integer | **PK**, autoincrement | 自增主键 |
| `cpu_usage` | Float | — | CPU 使用率 (%) |
| `gpu_usage` | Float | nullable | GPU 使用率 (%) |
| `memory_usage` | Float | — | 内存使用率 (%) |
| `disk_usage` | Float | — | 磁盘使用率 (%) |
| `network_rx` | Float | — | 网络接收 (Mbps) |
| `network_tx` | Float | — | 网络发送 (Mbps) |
| `video_fps` | Float | — | 视频处理帧率 |
| `video_resolution` | String(20) | — | 视频分辨率 |
| `active_devices` | Integer | — | 活跃设备数 |
| `timestamp` | DateTime | **Index** | 采集时间 |

#### 4.2.7 model_configs — 模型与参数配置表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | Integer | **PK**, autoincrement | 自增主键 |
| `model_name` | String(50) | **Unique** | 模型名称 |
| `task_type` | String(30) | — | 任务类型: `detection` / `recognition` |
| `confidence_threshold` | Float | — | 置信度阈值 |
| `iou_threshold` | Float | — | IoU 阈值 |
| `enabled` | Boolean | — | 是否启用 |
| `params` | JSON | — | 额外参数 (JSON 格式) |
| `updated_at` | DateTime | — | 更新时间 |

---

## 5. 统一接口设计

所有的 HTTP 和 WebSocket 通信均收敛于云端主服务的 **8000 端口**。

### 5.1 WebSocket 端点

| 路径 (Path) | 传输方向 | 数据格式 | 用途说明 |
|-------------|----------|----------|----------|
| `ws://host:8000/ws/stream/{device_id}` | 边端 → 云端 | Binary (JPEG) | 接收边端设备的实时视频流推流 |
| `ws://host:8000/ws/dashboard` | 云端 → 前端 | JSON | 统一推送全量状态 (告警/识别结果/性能指标) |

### 5.2 RESTful API 端点

遵循标准 HTTP 动词与复数资源命名规范。

#### 白名单配置

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/whitelist` | 获取全部白名单车牌 |
| `POST` | `/api/whitelist` | 添加车牌到白名单 |
| `DELETE` | `/api/whitelist/{plate_id}` | 从白名单移除车牌 |

#### 统计图表

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/stats/vehicles?zone=xxx&from=xxx&to=xxx` | 车辆密度历史统计 |
| `GET` | `/api/stats/violations?type=parking&from=xxx&to=xxx` | 违章事件统计 |
| `GET` | `/api/stats/anomalies?from=xxx&to=xxx` | 路面异常统计 |
| `GET` | `/api/stats/system` | 系统资源实时 & 历史指标 |

#### 系统配置

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/configs/models` | 获取各模型参数配置 |
| `PUT` | `/api/configs/models` | 修改置信度、阈值等参数 |
| `GET` | `/api/configs/zones` | 获取禁停区域配置 |
| `PUT` | `/api/configs/zones` | 更新禁停区域多边形 |
| `GET` | `/api/health` | 系统健康检查 |

#### 通用响应结构

```json
{
    "code": 200,
    "message": "success",
    "data": { ... }
}
```

**错误响应**：

```json
{
    "code": 400,
    "message": "Invalid device_id",
    "data": null
}
```

---

## 6. 配置说明

系统配置集中于 `cloud_server/config.py`，关键参数如下：

### 6.1 数据库配置

```python
DATABASE_URL = "sqlite+aiosqlite:///./data/its.db"
```

### 6.2 视频流配置

```python
VIDEO_FRAME_WIDTH = 1280
VIDEO_FRAME_HEIGHT = 720
VIDEO_FPS = 15
VIDEO_STREAM_PORT = 8765       # WebSocket 视频流端口
```

### 6.3 AI 模型配置

```python
YOLO_MODEL_PATH = "models/yolov8n.pt"
YOLO_CONFIDENCE = 0.5          # YOLO 检测置信度阈值
PLATE_CONFIDENCE = 0.6         # 车牌识别置信度阈值
```

### 6.4 禁停区域配置

```python
NO_PARKING_ZONES = [
    {"name": "主路禁停区", "points": [(0.1, 0.1), (0.3, 0.1), (0.3, 0.4), (0.1, 0.4)]},
    {"name": "路口禁停区", "points": [(0.6, 0.5), (0.9, 0.5), (0.9, 0.9), (0.6, 0.9)]},
]
```

禁停区域使用**归一化坐标 (0-1)**，点位按顺时针或逆时针顺序构成封闭多边形。

### 6.5 系统阈值

| 参数 | 值 | 说明 |
|------|---|------|
| `PARKING_THRESHOLD` | 10 秒 | 违停判定时间阈值 |
| `CONGESTION_HIGH` | >8 辆 | 严重拥堵 |
| `CONGESTION_MEDIUM` | 5-8 辆 | 中度拥堵 |
| `CONGESTION_LOW` | 3-5 辆 | 轻度拥堵 |
| `DEVICE_TIMEOUT` | 30 秒 | 设备心跳超时判定离线 |
| `DATA_RETENTION_DAYS` | 30 天 | 历史数据保留天数 |
| `MONITOR_INTERVAL` | 5 秒 | 系统监控采集间隔 |

---

## 7. 部署与目录结构

### 7.1 目录结构

```
Intelligent_Transportation/
├── cloud_server/                    # 云端核心服务
│   ├── main.py                      # FastAPI 入口 (Port 8000)
│   ├── config.py                    # 全局配置
│   ├── pipeline/                    # 核心流水线引擎
│   │   ├── context.py               # FrameContext 定义
│   │   ├── engine.py                # 流水线执行调度器
│   │   └── nodes/                   # 各个独立的计算节点
│   │       ├── detection_node.py    # YOLOv8 车辆检测
│   │       ├── tracking_node.py     # SORT 目标跟踪
│   │       ├── ocr_node.py          # HyperLPR 车牌识别
│   │       ├── anomaly_node.py      # Depth Anything V2 异常检测
│   │       └── violation_node.py    # 违章判断 (违停/闯红灯等)
│   ├── api/                         # 接口层
│   │   ├── rest_routes.py           # 规范化的 REST API
│   │   ├── ws_routes.py             # WebSocket 管理
│   │   └── middleware.py            # CORS / 限流 / 鉴权中间件
│   └── database/                    # 数据层
│       ├── connection.py            # 数据库连接管理
│       └── orm_models.py            # SQLAlchemy ORM 模型
├── edge_device/                     # 手机端推流服务 (Flask)
│   ├── stream_app.py                # Flask 推流应用
│   └── templates/                   # HTML 推流页面
├── frontend/                        # 驾驶舱面板 (React + Vite)
│   ├── src/
│   │   ├── components/              # 可复用组件
│   │   ├── pages/                   # 各功能页面
│   │   │   ├── Dashboard.tsx        # 实时概览
│   │   │   ├── VehicleMonitor.tsx   # 车辆监控
│   │   │   ├── AnomalyAlerts.tsx    # 异常告警
│   │   │   └── SystemConfig.tsx     # 系统配置
│   │   └── services/
│   │       ├── api.ts               # REST API 调用
│   │       └── ws.ts                # WebSocket 连接
│   └── tailwind.config.js
├── models/                          # AI 模型权重文件
├── data/                            # 运行时数据目录
│   ├── its.db                       # SQLite 数据库文件
│   └── whitelist.json               # 白名单车牌 JSON
├── detect_vehicle.py                # 离线车辆检测脚本
├── detect_anomaly.py                # 离线异常检测脚本
├── requirements.txt                 # Python 依赖清单
├── README.md                        # 项目说明
└── docs/                            # 文档目录
    └── 系统设计方案v1.0.md
```